services:
  proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mysql_psql_proxy
    ports:
      - "5009:5009"
    volumes:
      - ./data:/app/data
    environment:
      RUST_LOG: info
    command: ["--web-ui"]
    restart: unless-stopped

